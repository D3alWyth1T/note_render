# Note Display Server Configuration
# Copy this file to .env and adjust values as needed

# Path to your notes directory (required)
NOTES_DIR=~/notes

# Default note to show on homepage (without .md extension)
DEFAULT_NOTE=homepage

# Server port
PORT=5000

# Allow viewing files outside NOTES_DIR (DANGEROUS - use with caution)
# Set to "true" to enable path traversal (access any file on the system)
# ALLOW_ALL_PATHS=false

# Notes to exclude from graph view (comma-separated, case-insensitive)
# GRAPH_EXCLUDE=yellowlist,todo
